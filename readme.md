 npm init
yarn add express
yarn add -D dotenv
const dotenv = require('dotenv');
dotenv.config();
yarn add nodemon
"scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  } -> in package.json


